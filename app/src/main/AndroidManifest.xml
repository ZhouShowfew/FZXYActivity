<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.steven.fzxyactivity">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".module.spalsh.SplashActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".module.main.View.Fragment.Activity.BottomMainActivity"
            android:label="校友圈"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".module.user.LoginActivity"
            android:label="登录"
            android:theme="@style/AppTheme.NoActionBar" />
        <!-- <activity android:name=".module.main.View.Fragment.Activity.MainActivity" /> -->
        <activity
            android:name=".module.spalsh.WelcomeGuideActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".module.user.RegisterActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".module.activitydetail.ActivityDeatailActivity"
            android:theme="@style/AppTheme.NoActionBar"
            />
        <activity
            android:name=".module.newactivity.NewActivityActivity"
            android:theme="@style/AppTheme.NoActionBar"
            />
    </application>

</manifest>